{
  "id": "definition",
  "name": "Definition Essay",
  "purpose": "Establishes what a term means beyond its dictionary line, especially for terms that are contested, abstract, or narrower/wider than common usage assumes.",
  "subtype_of": "expository",
  "components": [
    { "ref": "baseline-definition", "required": true },
    { "ref": "extended-definition", "required": true },
    { "ref": "conclusion", "required": false }
  ],
  "pitfalls": [
    "A circular definition — the term defined using itself or a bare synonym with no added content.",
    "Extended definition that never says what the term is NOT, leaving its boundary unclear.",
    "Stopping at the baseline definition, which does no more work than a dictionary would."
  ]
}
