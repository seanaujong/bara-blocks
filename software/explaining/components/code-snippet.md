# Code Snippet

A short, runnable excerpt that shows the exact call or configuration the reader needs — not a
restatement in prose of what the code does. Trimmed to the minimum that still runs; a snippet with
unrelated setup code buries the one line that matters.

> ```kotlin
> val receipt = checkoutClient.scan(barcode = "9780143127550")
> ```
