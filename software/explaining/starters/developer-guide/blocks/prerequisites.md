# Prerequisites

What the reader needs installed, configured, or already true before the first step will work — a
tool version, an account, an environment variable, an existing dependency. Stated up front so a
reader doesn't discover a missing prerequisite three steps in.

> "Requires the checkout SDK v2.3+ and a library API key set as `LIBRARY_API_KEY`."
